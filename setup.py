from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The ensemble transformer is a neural network module for processing of ensemble weather forecasting data',
    author='Tobias Finn',
    license='MIT',
)

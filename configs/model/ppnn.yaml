defaults:
  - embedding: default
  - optimizer: adam
  - scheduler: plateau

_recursive_: False
_target_: ens_transformer.models.PPNNet
n_transformers: 1
output_channels: 2
loss_str: crps
learning_rate: ${learning_rate}

transformer:
  kernel_size: 3
  activation: torch.nn.ReLU
